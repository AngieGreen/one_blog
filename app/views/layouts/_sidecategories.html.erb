<!-- Blog Categories Well -->
<div class="well">
    <h4>All 52 Projects</h4>
    <div class="row">
        <div class="col-lg-6">
            <% @categories.each  do |category| %>
            <ul class="list-unstyled">
                <li><%= link_to category.name, category_path(category[:id]) -%>
                </li>
            </ul>
            <% end %>
        </div>
        <div class="col-lg-6">
            <% @categories.each do |category| %>
            <ul class="list-unstyled">
                <li><%= link_to category.name, category_path(category[:id]) -%>
                </li>
            </ul>
            <% end %>
        </div>
    </div>
    <!-- /.row -->
</div>