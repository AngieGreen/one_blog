<% provide(:title, "The Projects") %>

    <!-- Page Content -->

        <div class="row">

        <!-- Projects by Category-->
        <% @categories.each do |category| %>
            <div class="col-md-7">

                <ol>
                    <h1>IMAGE GOES HERE</h1>
            </div>
            <div class="col-md-5">
                    <h3><%= category.name %></h3>
                    <p><%= category.week %></p>
                    <p><%= category.minicontent %></p>
                    <span class="btn btn-primary"><%= link_to "FIX THIS LINK", categories_path %></span>

                </ol>
<hr>
            </div>
        <% end %>
        </div>
        <!-- /.row -->